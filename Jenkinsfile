pipeline {
   agent any
           // label 'jenkins-slave-jnlp'
 stages {
        stage('Build') {
            steps {
                echo "Hello World"
                echo "Multiline shell steps works too"
            }
        }
    }
}
